${license}

#ifndef LIU_CC_PREPROCESS_REDUCES_H
#define LIU_CC_PREPROCESS_REDUCES_H

#include "tokenize/target.h"
#include "tokenize/context.h"

enum PREPROCESS_PRODUCT_RULE_ENUM {
  ${enum_reduces}
};

${reduces}

#endif  // LIU_CC_PREPROCESS_REDUCES_H
